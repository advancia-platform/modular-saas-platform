{
  "name": "advancia-email-worker",
  "version": "1.0.0",
  "description": "Cloudflare Email Worker for Advancia Pay Ledger - handles privacy@ and legal@ emails",
  "main": "dist/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "deploy:staging": "wrangler deploy --env staging",
    "build": "tsc",
    "tail": "wrangler tail",
    "test": "vitest"
  },
  "keywords": [
    "cloudflare",
    "email",
    "worker",
    "advancia"
  ],
  "author": "Advancia Pay Ledger",
  "license": "UNLICENSED",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.3.0",
    "wrangler": "^3.91.0",
    "vitest": "^1.0.0"
  },
  "dependencies": {
    "postal-mime": "^2.2.0",
    "resend": "^4.0.0"
  }
}
